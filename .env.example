# AI Provider API Keys (at least one required for AI features)
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: Default AI provider (google, openai, anthropic)
AI_PROVIDER=

# Repository scanning settings
# Comma-separated list of absolute paths to scan for git repositories
# Example: /Users/me/code,/Users/me/projects
# Defaults to $HOME if not set
REPO_SCAN_ROOT=/Users/me/code
REPO_SCAN_MAX_DEPTH=3        # How deep to scan for repos
